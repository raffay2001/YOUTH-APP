import {StyleSheet} from 'react-native';

export const postWrapperStyles = StyleSheet.create({
  container: {
    backgroundColor: '#FFFFFF',
    marginTop: 6,
    marginHorizontal: 6,
    borderTopLeftRadius: 16,
    borderTopEndRadius: 16,
    paddingVertical: 12,
    paddingHorizontal: 16,
    rowGap: 10,
  },
});
